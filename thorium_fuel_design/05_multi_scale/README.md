# 05_multi_scale

当前仓库未包含组件级/堆芯级专门输入与结果。

已完成：

- 建立 `assembly/` 与 `fullcore/` 对比文档入口；
- 保留与现有 pin-cell 数据的衔接说明。

建议：基于 `scripts/input_generation/templates/template_assembly.ser` 与 `template_core.ser` 逐步补齐。
