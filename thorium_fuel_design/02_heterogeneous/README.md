# 02_heterogeneous

现有仓库数据集中（A/B/C/D）均为 `layout_base=uniform`，暂无异质轴向分段原始算例。

已完成的填充：

- 建立了目标目录与 `scan_matrix.csv` 模板；
- 将可执行能力保留在 `thorium_fuel_design/scripts/` 适配层；
- 后续可用 `scripts/input_generation/generate_from_matrix.py` 生成异质输入并补齐本目录。
